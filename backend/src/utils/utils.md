# utils 目录说明

- 作用：通用工具函数与辅助模块（如 `auth.py`、`config_loader.py`），为路由与服务层提供复用能力。
- 责任边界：保持无状态、纯函数化，避免引入业务耦合或全局可变状态；慎用单例与缓存以防循环依赖。
- 命名与结构：按功能聚合模块，保持清晰输入/输出类型；跨模块公用常量/校验函数可集中在此。
- 测试建议：为纯函数补充单元测试或示例，覆盖正常与异常路径；涉及外部资源时提供模拟或可配置注入。

# db 目录说明

- 作用：使用 SQLAlchemy 管理会话、订单、持仓等持久化模型（`models.py`），并通过 `session_storage.py` 封装存取逻辑。
- 责任边界：仅负责数据持久化和模型定义，不承载业务流程或路由逻辑。
- 结构命名：模型枚举/字段与交易域保持一致（session/order/position），迁移或字段变更需保持向后兼容或提供升级脚本。
- 测试与数据安全：测试优先使用内存库或临时 SQLite 文件，避免污染真实数据库；执行破坏性变更前做好备份。

# config 目录说明

- 作用：集中管理环境变量、默认配置与资源目录校验（见 `settings.py`），为服务层/路由提供统一入口。
- 责任边界：仅处理配置，不承载业务逻辑；读取 `.env`/外部配置时避免写入副作用。
- 结构命名：新的配置项请集中到 `settings.py`，使用清晰的前缀与默认值；更新后同步 `.env.template` 与相关文档。
- 安全与合规：禁止将真实密钥、账户信息写入代码库；敏感配置必须通过环境变量或外部文件注入。

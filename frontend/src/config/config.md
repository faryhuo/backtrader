# config 目录说明

前端运行时配置、常量与枚举聚合目录。

## 功能职责（Functional）
- 存放路由表、菜单配置、常量、默认参数等前端侧配置。
- 作为组件/页面的共享依赖入口。

## 非功能性要求（Non-Functional）
- 可维护性：配置需集中管理，避免散落在组件里。
- 可读性：配置命名语义化，提供必要注释或文档。

## 约定与规范
- 不在此目录存放秘密或环境敏感值，环境变量放 `frontend/.env*`。
- 新增配置需考虑向后兼容与默认值。


# hooks 目录说明

自定义 React Hook 目录。

## 功能职责（Functional）
- 抽取跨组件复用的状态与副作用逻辑。
- 典型场景：表单、轮询、WebSocket、权限、分页等。

## 非功能性要求（Non-Functional）
- 可测试性：Hook 设计需便于单测（纯逻辑可独立测试）。
- 性能：避免在 Hook 内创建不必要的新对象/函数导致重渲染。

## 约定与规范
- Hook 命名以 `use` 开头，参数与返回值保持稳定、可预测。
- 不在 Hook 内直接操纵 DOM，除非明确与展示无关且有必要。


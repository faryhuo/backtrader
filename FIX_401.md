# 修复 401 Unauthorized 错误 - 快速指南

## ✅ 已完成修复

以下配置已经修正：

### 后端配置 (`backend/.env`)
```env
LOGTO_AUDIENCE=http://localhost:8000  ✅ 正确
```

### 前端配置 (`frontend/.env`)
```env
VITE_API_RESOURCE=http://localhost:8000  ✅ 正确
```

### 前端已重新构建
```bash
✅ npm run build 完成
✅ 文件已复制到 backend/resources/frontend/
```

## 🚀 现在需要您做的事情

### 1. 配置 Logto 控制台 ⭐ 最重要

访问 https://logto.fary.chat/console 并完成以下配置：

#### Step 1: 创建 API Resource（如果还没创建）

1. 点击左侧菜单 **API Resources**
2. 点击右上角 **Create API Resource**
3. 填写信息：
   - **Name**: `Backtrader API`（任意名称）
   - **API Identifier**: `http://localhost:8000` ⚠️ 必须完全一致
4. 点击 **Create**

#### Step 2: 将 API Resource 关联到前端应用

1. 点击左侧菜单 **Applications**
2. 找到并点击前端应用：`ro4uk4fd2czd7cyx3wcbm`
3. 切换到 **API Resources** 标签
4. 点击 **Add API Resource**
5. 选择刚才创建的 API Resource（标识符为 `http://localhost:8000`）
6. 点击 **Save** 或 **Add**

#### Step 3: 验证其他设置（可选但建议）

在同一个前端应用页面，检查：

**Redirect URIs** 标签：
- ✅ 包含 `http://localhost:8000/callback`

**Post Logout Redirect URIs**：
- ✅ 包含 `http://localhost:8000`

**CORS Allowed Origins**：
- ✅ 包含 `http://localhost:8000`

### 2. 重启后端服务器

```bash
cd backend
python main.py
```

您应该看到：
```
Logto authentication initialized: https://logto.fary.chat
```

### 3. 清除浏览器缓存并重新登录

#### 方法 1: 使用浏览器控制台（推荐）

1. 打开 `http://localhost:8000`
2. 按 F12 打开开发者工具
3. 在 **Console** 标签中执行：
   ```javascript
   localStorage.clear();
   sessionStorage.clear();
   location.reload();
   ```

#### 方法 2: 手动清除

1. 按 `Ctrl + Shift + Delete`
2. 选择"Cookies 和其他站点数据"
3. 时间范围选择"所有时间"
4. 点击"清除数据"

### 4. 重新登录测试

1. 访问 `http://localhost:8000`
2. 看到登录页面
3. 点击 **Sign In**
4. 输入 Logto 账户凭据
5. 登录成功后应该跳转到 `/app`
6. 打开浏览器 DevTools → Network 标签
7. 刷新页面，观察 `/api/strategies` 请求
8. **应该返回 200 OK** ✅

## 🔍 验证步骤

### 检查 Network 请求

打开 DevTools → Network，找到 `/api/strategies` 请求，检查：

**Request Headers** 应该包含：
```
Authorization: Bearer eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ...
```

**Response** 应该是：
```
Status Code: 200 OK
```

### 如果仍然是 401

1. **复制 Authorization header 中的 token**
2. **运行调试工具**：
   ```bash
   cd backend
   python debug_token.py <复制的token>
   ```
3. **检查输出中的 Audience (aud)**：
   - 应该是：`http://localhost:8000`
   - 如果不是，说明 Logto 控制台的 API Resource 配置不正确

## 📋 故障排除清单

如果还有问题，逐一检查：

- [ ] **Logto 控制台**：API Resource 已创建，identifier 是 `http://localhost:8000`
- [ ] **Logto 控制台**：前端应用已关联上述 API Resource
- [ ] **后端 .env**：`LOGTO_AUDIENCE=http://localhost:8000`（无 `/api`）
- [ ] **前端 .env**：`VITE_API_RESOURCE=http://localhost:8000`（无 `/api`）
- [ ] **后端已重启**：`python main.py`
- [ ] **前端已重新构建**：`npm run build` + 文件已复制
- [ ] **浏览器缓存已清除**：localStorage 和 sessionStorage 已清空
- [ ] **已重新登录**：登出后重新 Sign In

## 🎯 快速验证命令

运行验证脚本检查配置：
```bash
python verify_config.py
```

应该显示：
```
✅ All configurations are correct!
```

## 📞 还是不行？

如果完成上述所有步骤后仍然 401，请提供：

1. **Token 解码结果**：
   ```bash
   python debug_token.py <your-token>
   ```

2. **浏览器控制台错误消息**（如果有）

3. **后端日志**（token 验证相关的输出）

4. **Logto 控制台截图**：
   - API Resource 配置页面
   - 前端应用的 API Resources 关联页面

---

**重要提示**: 最常见的问题是忘记在 Logto 控制台关联 API Resource！请务必完成 Step 2！
